// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

/// User model - Stores user profile data
/// The id matches the Supabase Auth user id for easy lookups
model User {
  id           String   @id @default(uuid())
  email        String   @unique
  passwordHash String   // Format: "supabase:{supabase_user_id}" (placeholder)
  role         String   @default("student")
  status       String   @default("active")
  firstName    String?
  lastName     String?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  // Relations for waste reports (future)
  // wasteReports WasteReport[]
  // upvotes      ReportUpvote[]

  @@map("users")
}

// Future models for CleanMap features:

// model WasteReport {
//   id          String   @id @default(uuid())
//   userId      String
//   type        String   // "plastic", "organic", "metal", etc.
//   status      String   @default("reported") // "reported", "acknowledged", "cleaned"
//   latitude    Float
//   longitude   Float
//   description String?
//   photoUrl    String?
//   createdAt   DateTime @default(now())
//   updatedAt   DateTime @updatedAt
//   
//   user        User     @relation(fields: [userId], references: [id])
//   upvotes     ReportUpvote[]
//   
//   @@map("waste_reports")
// }

// model ReportUpvote {
//   id        String   @id @default(uuid())
//   userId    String
//   reportId  String
//   createdAt DateTime @default(now())
//   
//   user      User        @relation(fields: [userId], references: [id])
//   report    WasteReport @relation(fields: [reportId], references: [id])
//   
//   @@unique([userId, reportId])
//   @@map("report_upvotes")
// }
